declare module 'js-md5' {
  /**
   * Function hashing with MD5. Not suitable for any cryptographic uses.
   * @param s string to hash
   * @returns MD5 hash as hex string
   */
  export default function (s: string): string
}
